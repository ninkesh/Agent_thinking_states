/* RemoteOverlay — Phase 1.1
   D-pad, arrow hints, OK/Back labels, and Restart buttons removed.
   Keyboard shortcuts (Arrow Keys, Enter, Escape, R) remain functional via
   the global keydown handlers registered in each screen component.
   This shell is kept so import sites don't break.
*/
export default function RemoteOverlay() {
  return null;
}
